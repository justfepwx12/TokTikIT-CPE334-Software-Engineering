services:
  postgres:
    image: postgres:16-alpine
    container_name: toktikit-db
    environment:
      POSTGRES_USER: toktikit
      POSTGRES_PASSWORD: toktikit
      POSTGRES_DB: toktikit
    ports:
      - "15432:5432"